#include "main.h"
/**
 * add - function adds two integers
 * @i: An input character
 * @j: Another input character
 * Description: function simply adds 2 integers
 * character must be a digit
 * Return: the sum of 2 integers
 */
int add(int i, int j)
{
return (i + j);
}
