#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * @argc: counter
 * @argv: values
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
if (argc > 0)
printf("%s\n", argv[0]);
return (0);
}
