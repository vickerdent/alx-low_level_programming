#include "main.h"
#include <string.h>

/**
 * _strlen - a function that takes a string
 * @s: An string input
 * Return: the value of a string
 */

int _strlen(char *s)
{
int len;
len = strlen(s);
return (len);
}
