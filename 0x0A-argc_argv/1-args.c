#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * @argc: counter
 * @argv: values
 * Return: Always 1 or 0
 */
int main(int argc, char *argv[])
{
if (argv[0])
printf("%d\n", argc - 1);
return (0);
}
