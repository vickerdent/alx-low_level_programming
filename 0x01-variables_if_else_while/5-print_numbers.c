#include <stdio.h>
/* more headers goes there */

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int c = 0;
while (c < 10)
{
printf("%d", c);
c++;
}
printf("\n");
return (0);
}
