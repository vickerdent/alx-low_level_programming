#include "main.h"

/**
 * _abs - function computes the absolute value of an integer
 * @c: An input character
 * Description: function simply returns absolute value
 * integer values only
 * Return: absolute value
 */
int _abs(int c)
{
if (c >= 0)
return (c);
else
return (c * -1);
}
